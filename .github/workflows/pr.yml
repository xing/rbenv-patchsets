on:
  push: # DELETE ME
  workflow_dispatch:
  schedule:
    - cron: "0 8 * * 1-5"
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: 3
          bundler-cache: true
      - run: bundle exec ./commit-message
      - run: echo '::set-output name=commit-message::$(bundle exec ./commit-message)'
      # - name: ./update
      #   run: ./update
      # - name: create PR
      #   uses: peter-evans/create-pull-request@v3
      #   with:
      #     # commit-message
      #     draft: true
